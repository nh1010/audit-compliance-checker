services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - chroma-data:/app/data/chroma
      - ./backend/policies:/app/policies

volumes:
  chroma-data:
